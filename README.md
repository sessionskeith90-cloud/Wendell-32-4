# Wendell Core 32/4

Rust forensic intelligence platform with 32 agents (16 primary + 16 shadow) and 4 core components.

## Build
- Install Rust: https://rustup.rs
- Run `cargo build --release`
- Executables in `target/release/`

## Run
Create `channel.txt` and run the five executables.
